type AuthenticateGoogleUserPayload {
  id: ID!
  displayName: String!
  email: String!
}

extend type Mutation {
  authenticateGoogleUser(
    googleToken: String!,
    displayName: String!,
    email: String!
  ): AuthenticateGoogleUserPayload!
}